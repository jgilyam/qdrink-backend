import {describe, expect, test, jest, beforeEach, it} from '@jest/globals';
import { DebitCreditKind, SaleChannel, AccountingCodeKind } from '../../../../../src/core/accounting-code/domain/accounting.code.entity';
import { AccounitngCodeRepository } from '../../../../../src/core/accounting-code/infrastructure/mongo/mongo.accounting.code.repository';
/*


const accountingCodes = [
    {
        name: "Carga de credito a online",
        kind: DebitCreditKind.CREDIT,
        saleChannel: SaleChannel.ONLINE,
        accountingCodeKind: AccountingCodeKind.CREDIT_PAYMENT
    }
]

describe('sum module', () => {
    let accounitngCodeRepository: AccounitngCodeRepository;
    
    beforeEach(()=>{
        accounitngCodeRepository = new AccounitngCodeRepository()
    });

    it
  test('adds 1 + 2 to equal 3', () => {
    
  });
});

*/